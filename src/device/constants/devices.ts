import { EnumItem } from '../../shared/types/enums';
import { DevicesEnum } from '../types/devices.enum';

export const DEVICES: EnumItem[] = [
  {
    value: DevicesEnum.ESP8266,
    text: 'ESP8266',
  },
];
